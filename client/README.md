# Setup

After following the top level README.md, just install dependencies and then run the client:

1. `npm install`
2. `npm run dev`
3. Open the the url that's shown after starting the client (ex: http://localhost:5173/).
