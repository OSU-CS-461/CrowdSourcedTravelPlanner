# Database URL
# If using Prisma dev: Run npm run prisma:dev and copy the displayed DATABASE_URL
# If using local PostgreSQL: postgresql://postgres:your_password@localhost:5432/travelplanner?schema=public
# If using Neon (production): postgresql://<user>:<password>@<host>/<db>?sslmode=require
DATABASE_URL="prisma+postgres://localhost:51213/?api_key=your_api_key_here"

# JWT Secret for authentication
# Run in Windows PowerShell: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or run in Git Bash: openssl rand -hex 32
JWT_SECRET="your-jwt-secret-key-here"

# Node Environment
NODE_ENV="development"
